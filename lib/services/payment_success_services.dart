import 'package:rti_new_apps/services/base_services.dart';
import 'package:rti_new_apps/services/routes.dart';

class PaymentSuccessServices extends BaseService {
  Future verifyPayment(
    String receipt,
    String signature,
    String orderId,
    String paymentId,
  ) async {
    try {} catch (ex) {}
  }
}
