import 'package:flutter/material.dart';

homeAppBarWidget() {
  return AppBar(
    leading: const Icon(Icons.rtt),
    actions: [
      IconButton(
        onPressed: () {},
        icon: const Icon(
          Icons.more_horiz_sharp,
        ),
      ),
    ],
  );
}
